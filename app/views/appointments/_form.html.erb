<table class="attr">
  <tr>
    <th><%= form.label :department_id ,"時間" %></th>
    <td><%= form.collection_select :department_id,Department.all,:id,:name%></td>
  </tr>
   <tr>
    <th><%= form.label :period_id, "時間" %></th>
    <td><%= form.collection_select :period_id,Period.all,:id,:time%></td>
  </tr>
</table>
